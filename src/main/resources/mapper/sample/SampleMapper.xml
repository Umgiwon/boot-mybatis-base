<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bootmybatisbase.api.sample.mapper.SampleMapper">

    <!-- 샘플 목록 조회 -->
    <select id="getSampleList" resultType="com.bootmybatisbase.api.sample.vo.SampleVO">
        SELECT A.SAMPLE_SN
             , A.TITLE
             , A.CONTENT
             , A.CREATED_USER
             , A.CREATED_DATE
             , A.UPDATED_USER
             , A.UPDATED_DATE
        FROM initial.TB_SAMPLE A
        ORDER BY A.CREATED_USER DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 샘플 전체목록 카운트 조회 -->
    <select id="countSampleList" resultType="long">
        SELECT COUNT(*)
          FROM initial.TB_SAMPLE
    </select>

    <!-- 샘플 단건 조회 -->
    <select id="getSample" resultType="com.bootmybatisbase.api.sample.vo.SampleVO">
        SELECT A.SAMPLE_SN
             , A.TITLE
             , A.CONTENT
             , A.CREATED_USER
             , A.CREATED_DATE
             , A.UPDATED_USER
             , A.UPDATED_DATE
          FROM initial.TB_SAMPLE A
         WHERE A.SAMPLE_SN = #{sampleSm}
    </select>

</mapper>